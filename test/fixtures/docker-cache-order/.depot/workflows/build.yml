name: Docker build
on: workflow_dispatch
permissions:
  contents: read
jobs:
  build:
    runs-on: depot-ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: depot/build-push-action@aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
        with:
          context: .
